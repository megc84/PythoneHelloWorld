import random


colors = ['red', 'blue', 'green', 'yellow', 'cyan'];
print (colors[0])
print ( len(colors))

for c in colors:
	print ("Nice " +  c)

print (random.randint(0,9))


for d in range(random.randint(2,9)):

		print ("Me imprimo " + d + " veces")